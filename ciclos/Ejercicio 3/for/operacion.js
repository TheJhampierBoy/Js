function conteo(pnumero){
    let numero=pnumero
    let conteor

    for(let multiplicacion=1; multiplicacion<=numero; multiplicacion++){
        resultado=multiplicacion*numero
        console.log(numero + "x" + multiplicacion + "=" + resultado)

    }
    return(conteor)
    
}